{
  "name": "jobhub-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "JobHub static frontend — vanilla ES modules + Tailwind, no framework.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "tailwindcss -i ./css/input.css -o ./css/tailwind.css --watch",
    "serve": "npm run build && npx --yes serve@14 -l 5173 dist"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.13"
  }
}
