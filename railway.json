{
  "$schema": "https://railway.app/railway.schema.json",
  "//": [
    "Config for the JobHub API service. The worker service reuses this same",
    "Dockerfile but sets its own start command (python -m app.worker.run) in the",
    "Railway dashboard.",
    "",
    "Provision a PostgreSQL plugin in the same project; it injects DATABASE_URL.",
    "Set JWT_SECRET, SMTP_*, and any ADZUNA_*/JOOBLE_*/USAJOBS_* keys as service",
    "variables (see .env.example)."
  ],
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "//": "Migrations run from the container's own entrypoint (backend/docker-entrypoint.sh), not preDeployCommand -- Railway did not reliably invoke preDeployCommand for a service whose settings were first established via the dashboard, so this field is intentionally omitted rather than relied on.",
    "startCommand": "uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
