"""Route modules, one per resource area.

Populated across milestones:
    meta       health, version, countries, public source list   (milestone 1/5)
    jobs       search + filter + facets + detail + click beacons (milestone 5/7)
    taxonomy   public field/sub-field tree                       (milestone 5)
    auth       register / login / refresh / me                   (milestone 8)
    seeker     saved jobs, saved searches, alerts                 (milestone 9)
    employer   posting portal + dashboard                        (milestone 10)
    admin      taxonomy / sources / moderation / content / users (milestone 11)
"""
