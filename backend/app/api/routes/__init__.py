"""Route modules, one per resource area.

meta                health, version, countries, public source list
jobs                search + filter + facets + detail + click beacons
taxonomy            public field/sub-field tree
auth                register / login / refresh / me
seeker              saved jobs, saved searches, alerts
employer            posting portal + dashboard
admin               employer + job moderation queue
admin_taxonomy      field/sub-field CRUD, remap, uncategorized queue
admin_sources       source registry, health, run-now
admin_jobs          general listing management (edit/hide/categorize)
admin_users         user + admin-account management
admin_content       site content + announcements (+ public reads)
admin_analytics     traffic / search / signup / posting summary
"""
